diff --git a/node_modules/expo-camera/android/src/main/java/expo/modules/camera/CameraViewModule.kt b/node_modules/expo-camera/android/src/main/java/expo/modules/camera/CameraViewModule.kt
index 21382f8..0741b60 100644
--- a/node_modules/expo-camera/android/src/main/java/expo/modules/camera/CameraViewModule.kt
+++ b/node_modules/expo-camera/android/src/main/java/expo/modules/camera/CameraViewModule.kt
@@ -390,6 +390,18 @@ class CameraViewModule : Module() {
         }
       }
 
+      Prop("focusPointX") { view, x: Float? ->
+        view.focusPointX = x ?: 0.5f
+      }
+
+      Prop("focusPointY") { view, y: Float? ->
+        view.focusPointY = y ?: 0.5f
+      }
+
+      Prop("focusKey") { view, key: Int? ->
+        view.focusKey = key ?: 0
+      }
+
       Prop("ratio") { view, ratio: CameraRatio? ->
         ratio?.let {
           if (view.ratio != it) {
diff --git a/node_modules/expo-camera/android/src/main/java/expo/modules/camera/ExpoCameraView.kt b/node_modules/expo-camera/android/src/main/java/expo/modules/camera/ExpoCameraView.kt
index 999a22c..89e1a2a 100644
--- a/node_modules/expo-camera/android/src/main/java/expo/modules/camera/ExpoCameraView.kt
+++ b/node_modules/expo-camera/android/src/main/java/expo/modules/camera/ExpoCameraView.kt
@@ -180,6 +180,27 @@ class ExpoCameraView(
       }
     }
 
+  // Normalized tap-to-focus point (0..1 of the preview view). With focusKey == 0
+  // the camera stays on continuous AF; any positive key runs a one-shot AF+AE
+  // metering at the point that locks until focusKey returns to 0.
+  var focusPointX: Float = 0.5f
+  var focusPointY: Float = 0.5f
+  var focusKey: Int = 0
+    set(value) {
+      field = value
+      // Post to the view queue so the point props from the same JS prop batch
+      // are guaranteed to be applied before metering runs.
+      post {
+        camera?.cameraControl?.let {
+          if (field == 0) {
+            it.cancelFocusAndMetering()
+          } else {
+            startFocusMetering()
+          }
+        }
+      }
+    }
+
   var videoQuality: VideoQuality = VideoQuality.VIDEO1080P
     set(value) {
       field = value
@@ -630,24 +651,42 @@ class ExpoCameraView(
 
   private fun startFocusMetering() {
     camera?.let {
+      // The preview can detach between the post{} deferral and this call —
+      // a null display would throw inside DisplayOrientedMeteringPointFactory.
+      val display = previewView.display ?: return
       val meteringPointFactory = DisplayOrientedMeteringPointFactory(
-        previewView.display,
+        display,
         it.cameraInfo,
         previewView.width.toFloat(),
         previewView.height.toFloat()
       )
       val action = FocusMeteringAction.Builder(
-        meteringPointFactory.createPoint(1f, 1f),
-        FocusMeteringAction.FLAG_AF
+        meteringPointFactory.createPoint(
+          previewView.width * focusPointX,
+          previewView.height * focusPointY
+        ),
+        FocusMeteringAction.FLAG_AF or FocusMeteringAction.FLAG_AE
       )
+        // No auto-cancel: the metering stays locked until focusKey goes back to 0.
+        .disableAutoCancel()
         .build()
       it.cameraControl.startFocusAndMetering(action)
     }
   }
 
   private fun setCameraZoom(value: Float) {
-    val maxZoomRatio = camera?.cameraInfo?.zoomState?.value?.maxZoomRatio ?: 1f
-    val targetZoomRatio = max(1f, min(maxZoomRatio, value.coerceIn(0f, 1f) * maxZoomRatio))
+    // Camen patch: map the normalized 0..1 linearly across the sensor's FULL
+    // range [minZoomRatio, maxZoomRatio]. Upstream floors at 1x ("max(1f, …)")
+    // and multiplies the normalized value by the max alone, which made the
+    // 0.6x ultrawide stop unreachable and mis-scaled every stop in between.
+    val zoomState = camera?.cameraInfo?.zoomState?.value
+    val minZoomRatio = zoomState?.minZoomRatio ?: 1f
+    val maxZoomRatio = zoomState?.maxZoomRatio ?: 1f
+    val targetZoomRatio =
+      min(
+        maxZoomRatio,
+        max(minZoomRatio, minZoomRatio + value.coerceIn(0f, 1f) * (maxZoomRatio - minZoomRatio)),
+      )
     camera?.cameraControl?.setZoomRatio(targetZoomRatio)
   }
 
@@ -657,6 +696,10 @@ class ExpoCameraView(
         CameraState.Type.OPEN -> {
           onCameraReady(Unit)
           setTorchEnabled(enableTorch)
+          // Camen patch: a session recreate (facing/mode switch) silently
+          // drops a pending focus lock — re-issue the metering on the fresh
+          // camera so the JS-side ring matches reality.
+          if (focusKey > 0) post { startFocusMetering() }
         }
 
         else -> {}
diff --git a/node_modules/expo-camera/build/Camera.types.d.ts b/node_modules/expo-camera/build/Camera.types.d.ts
index 0ee50ec..afa0eaf 100644
--- a/node_modules/expo-camera/build/Camera.types.d.ts
+++ b/node_modules/expo-camera/build/Camera.types.d.ts
@@ -348,6 +348,25 @@ export type CameraViewProps = ViewProps & {
      * @platform ios
      */
     autofocus?: FocusMode;
+    /**
+     * Normalized x (0..1) of the tap-to-focus point within the preview.
+     * Applied when `focusKey` changes. (Camen Android patch.)
+     * @platform android
+     */
+    focusPointX?: number;
+    /**
+     * Normalized y (0..1) of the tap-to-focus point within the preview.
+     * Applied when `focusKey` changes. (Camen Android patch.)
+     * @platform android
+     */
+    focusPointY?: number;
+    /**
+     * Monotonic counter: any positive value runs one-shot AF/AE metering at
+     * (focusPointX, focusPointY) and locks it; 0 releases back to continuous
+     * autofocus. (Camen Android patch.)
+     * @platform android
+     */
+    focusKey?: number;
     /**
      * A boolean that determines whether the camera should be active.
      * Useful in situations where the camera may not have unmounted but you still want to stop the camera session.
